pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build'
		bat "echo Build"
		bat "dir"
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy'
      }
    }

  }
}
